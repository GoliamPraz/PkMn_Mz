services:
  - type: web
    name: pkmn-mz
    env: docker
    plan: free
    dockerfile: ./Dockerfile
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
